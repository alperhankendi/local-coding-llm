# Task 01, FizzBuzz baseline

Smallest possible coding task. If this fails, the validate-coding-tasks pipeline itself or the model setup is broken, not the model's coding ability.

The model is asked to produce a single Python file `fizzbuzz.py` that prints the FizzBuzz sequence from 1 to 100. The verify script runs the file and checks output line count and a few specific lines.
