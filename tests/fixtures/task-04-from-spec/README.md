# Task 04, build a small CLI from a short spec

Given a short specification, the model produces `csv_group.py`, a CLI that reads a CSV from stdin and groups rows by a column. Verify runs the CLI against a fixed CSV input and checks the output. Also tests negative path (missing column should error).
